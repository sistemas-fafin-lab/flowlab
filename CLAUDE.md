## Agent skills

### Issue tracker

Issues locais em markdown, sob `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Labels canônicas padrão (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` na raiz. See `docs/agents/domain.md`.
