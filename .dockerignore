# Dependências e artefatos de build (reinstalados/recompilados na imagem).
node_modules
dist
dist-api
*.tsbuildinfo

# Git / CI
.git
.gitignore
.github

# Segredos e envs locais (NUNCA entram na imagem — vêm via env_file no compose).
**/.env
**/.env.*
!.env.example

# WAHA (não faz parte da imagem FlowLAB)
sessions
media

# Misc
*.log
.DS_Store
Dockerfile
docker-compose.yml
docker-compose.yaml
